{
  "name": "aim-recipes",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "16.x",
    "npm": "8.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm install --prefix server",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "Ahmed Tamer Fahmy",
  "license": "ISC"
}
